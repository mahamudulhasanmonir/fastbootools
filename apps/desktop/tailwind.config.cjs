/**
 * Tailwind CSS configuration for the desktop app.
 * Uses JIT mode and scans all source files for class names.
 */
module.exports = {
  content: ['./src/**/*.{js,ts,jsx,tsx}', './index.html'],
  theme: {
    extend: {},
  },
  plugins: [],
};